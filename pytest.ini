# SPDX-FileCopyrightText: 2026 Vortex contributors
# SPDX-License-Identifier: Apache-2.0

[pytest]
markers =
    real_ray: starts or connects to a real Ray runtime
    ray_cluster_owner: owns Ray cluster startup and teardown
    ray_fault: injects a real Ray worker or control-plane failure
    timeout: documents the expected upper bound for a test
